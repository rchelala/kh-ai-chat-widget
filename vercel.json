{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "functions": {
    "api/chat.ts": { "includeFiles": "knowledge/**" }
  },
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/$1" }]
}
